#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#include <string>

using namespace std;

int main()
{
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    int n, q;
    std::string HRML, tmp;
    std::vector<std::string> single_line;
    std::cin >> n >> q;
    std::cin >> HRML;
    std::stringstream ss(HRML);
    std::string word;
    while (std::getline(ss, word, ' '))
    {
    }

    for (int i = 0; i < q; i++)
    {
    }
    return 0;
}
