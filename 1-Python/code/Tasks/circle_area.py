#! /usr/bin/python3
from math import pi

#Write a Python program which accepts the radius of a circle from the user and compute the area.
radius = float(input("Enter the radius of the circle "))

print("the area of the circuit is " + str((radius**2)*pi))